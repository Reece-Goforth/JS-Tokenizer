# JS Tokenizer
This program takes a string and outputs an Abstract Syntax Tree (AST) of tokens in a JSON format.
Right now there is no front end for the user to interact with, there is only a test file.

# Features:
- Numeric Literal support
- String literal support
